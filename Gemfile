source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in FundingCircle.gemspec
gemspec


# dev
group :development do
  gem 'pry'
  gem 'rubocop'
end
